datasets:
  - "ml_20m"

val_schemes:
  - "time_split"

models:

  - report_file_name: baselines
    cls: PopularModel
    comment: _
    fixed_parameters:
      period.days: 7
    search_parameters:

  - report_file_name: baselines
    cls: SASRecModel
    comment: _vanilla_
    fixed_parameters:
      verbose: 1
      deterministic: True
      get_val_mask_func: src.models.transformers.trainer.get_val_mask_func_val_users_2048
      get_trainer_func: src.models.transformers.trainer.get_trainer_val_loss
      data_preparator_type: src.models.rl.data_preparators.RewardedSASRecDataPreparator
      data_preparator_kwargs.reward_fn: rating_threshold
      data_preparator_kwargs.reward_threshold: 3.5
      data_preparator_kwargs.reward_rating_col: rating
      session_max_len: 200
      loss: BCE
      n_negatives: 1
      dropout_rate: 0.2
      n_factors: 256
      n_blocks: 4
      n_heads: 8
    search_parameters:
